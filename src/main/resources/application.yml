spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/blog
    username: postgres
    password: postgres

  liquibase:
    enabled: true
    change-log: classpath:/db/changelog/db.changelog.master.xml
server:
  port: 8080